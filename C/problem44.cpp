#include <stdio.h>
int main()
{
	int i,j,n,n1;
	scanf("%d",&n);
	for (i=1;i<=n;i++){
		scanf("%d",&n1);
		for (j=1;j<=n1;j++){
			int a;
			scanf("%d %d %d",&a,&b,&c);
			if (a<=18 && a>1 && b<=18 && b>1 && c<=18 && c<1){
				
			}
		}
	}
	return 0;
}
